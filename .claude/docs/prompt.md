# BlushBite Prompt Log

Running record of prompts generated by the `blushbite-prompt-engineer` agent, one
entry per feature request. Each entry is split by which repo the prompt targets —
prompts are self-contained and meant to be pasted into a **fresh** Claude Code
session in that repo, with no memory of this log or of the other prompt in the pair.

New entries go at the top. Never edit a past entry's prompt text after the fact —
if a prompt turned out wrong, add a note under it rather than rewriting history.

---

## 2026-07-25 — Notification system: segment by actor type (Admin / Dreamer / Companion)

**Ask:** divide notifications into per-category toggles for three actor types —
Admin (content interactions, new content, companion bookings, companion subscription
purchases), Dreamers (own-content interactions, nearby recommendations), Companion
(bookings, interactions) — with a Notifications toggle section in each actor's
settings page.

**Key findings from grounding investigation (see full detail in each prompt below):**
- `notification_preferences` table **already exists live** in the shared DB — not
  built by either prompt below, discovered pre-existing with 4 columns
  (`content_interactions_enabled`, `new_content_enabled`, `companion_bookings_enabled`,
  `companion_subscriptions_enabled`) that map almost exactly to the 4 Admin
  categories, plus one seed row for a sentinel admin id
  (`00000000-0000-0000-0000-000000000001`). Origin unclear — presumably landed via
  blushbite_user tooling outside this log's tracking. `recipient_type` CHECK
  currently allows `'admin'`, `'user'` only — no `'companion'` yet.
- The real admin panel lives in **blushbite_user**, at
  `apps/web/app/(admin)/admin/*`, single-shared-secret auth via `ADMIN_SESSION_SECRET`
  / `admin_session` cookie, no per-admin identity. The `C:\Users\Ravi Desai\Downloads\BlushBite\`
  path referenced in blushbite.live's CLAUDE.md is stale/unreachable — do not chase it.
- WhatsApp-click-as-lead-event tracking (needed for Admin 1.3 / Dreamer 2.2 framing)
  **does not exist anywhere** — plain `<a href="wa.me/...">` with no onClick, no
  logging. Telegram contact channel doesn't exist on companion profiles at all.
  Both prompts below treat this as build-from-scratch, not wire-into-existing.
- Live `notif_type_check` (20 values) has already drifted from blushbite_user's
  Drizzle `db/schema.ts` (19 values, missing `boost_purchased`; its
  `notif_recipient_check` also doesn't yet list `'admin'` even though live DB does).
  Both prompts flag new `notification_type` additions rather than guessing, to avoid
  the two repos colliding on the same constraint independently.
- `lib/notify.ts`, `lib/push.ts`, and the companion notification UI in blushbite.live
  are **uncommitted in git** as of this entry — the companion-side prompt below
  treats them as landed working code to extend, but commit them before or alongside
  running that prompt so `git status` stays sane.

**Status:** prompts generated, not yet run.

### Project: blushbite.live (`C:\Users\judob\dev\dev26\landingpagebb-`)
Scope: companion-side toggles (categories 3.1 Bookings, 3.2 Interactions), plus the
migration step adding `'companion'` support to `notification_preferences`.

```
TASK: Extend blushbite.live's companion-side notification system with granular per-category toggles (bookings, interactions) in dashboard settings, and finish wiring the already-existing (but currently unused) `notification_preferences` table for admin categories that this repo is responsible for seeding data into.

REPO: blushbite.live — C:\Users\judob\dev\dev26\landingpagebb- (companion portal, Next.js 15 App Router, raw SQL via `pg`, npm, no ORM)

═══════════════════════════════════════════════════════════════════
PREREQUISITE — VERIFY LIVE DB STATE BEFORE WRITING ANY CODE
═══════════════════════════════════════════════════════════════════
Do not trust the numbers below blindly — re-run this read-only check first, since other
work may have landed since this prompt was generated:

    node -e "
    const fs = require('fs');
    const m = fs.readFileSync('.env.local','utf8').match(/^DATABASE_URL=(.*)\$/m);
    const { Pool } = require('pg');
    const pool = new Pool({ connectionString: m[1].trim(), ssl: { rejectUnauthorized: false } });
    (async () => {
      console.log(await pool.query(\"SELECT conname, pg_get_constraintdef(oid) def FROM pg_constraint WHERE conrelid='notifications'::regclass AND conname IN ('notif_type_check','notif_recipient_check')\"));
      console.log(await pool.query(\"SELECT conname, pg_get_constraintdef(oid) def FROM pg_constraint WHERE conrelid='notification_preferences'::regclass\"));
      console.log(await pool.query('SELECT * FROM notification_preferences LIMIT 5'));
      await pool.end();
    })();"

This repo's own `.env.local` (repo root) holds `DATABASE_URL` for the shared Railway Postgres
instance also used by blushbite_user — confirm nothing has since diverged from the ground truth
below.

═══════════════════════════════════════════════════════════════════
GROUND TRUTH — verified at prompt-generation time, sanity-check before depending on it
═══════════════════════════════════════════════════════════════════
1. `lib/notify.ts` — `createNotification({recipientType: 'companion'|'user', recipientId, notificationType, title, body, actionUrl?, metadata?})`. Inserts into shared `notifications` table, fires `sendPushToCompanion()` (from `lib/push.ts`) only when `recipientType === 'companion'`. This whole file plus `lib/push.ts`, `app/api/companions/notifications/route.ts`, `app/api/companions/push/{subscribe,unsubscribe}`, `components/dashboard/NotificationBell.tsx`, `public/sw.js` are currently **untracked in git** (`git status` at prompt time) — treat them as already-landed working code you extend, not code to recreate.
2. Live `notifications` table CHECK constraints (verified this run):
   - `notif_recipient_check`: `recipient_type IN ('user','companion','admin')`
   - `notif_type_check`: `notification_type IN ('profile_viewed','story_linked','booking_request','admin_approved','verification_complete','bridge_approved','story_like','story_save','story_comment','comment_reply','comment_like','booking_confirmed','booking_declined','booking_reminder','badge_awarded','payment_received','payout_processed','new_device_login','password_changed','boost_purchased')` — 20 values.
   - Note: blushbite_user's Drizzle `schema.ts` (a separate repo/session's responsibility) currently only has 19 of these (missing `boost_purchased`) and its local `notif_recipient_check` comment still says `IN ('user','companion')` without `'admin'` — this repo's migration already went further than that repo's model. Not your problem to reconcile — just don't be surprised if a future companion-side notification_type addition needs a matching Drizzle-side sync done elsewhere.
3. `notification_preferences` table **already exists live** (not yet referenced anywhere in either repo's code — confirmed via grep, zero hits):
   ```
   id uuid PK, recipient_type varchar NOT NULL, recipient_id uuid NOT NULL,
   content_interactions_enabled boolean NOT NULL DEFAULT true,
   new_content_enabled boolean NOT NULL DEFAULT true,
   companion_bookings_enabled boolean NOT NULL DEFAULT true,
   companion_subscriptions_enabled boolean NOT NULL DEFAULT true,
   created_at, updated_at timestamptz
   CHECK recipient_type IN ('admin','user')   -- 'companion' NOT yet allowed
   UNIQUE (recipient_type, recipient_id)
   ```
   One seed row exists: `recipient_type='admin', recipient_id='00000000-0000-0000-0000-000000000001'` (all four flags `true`). That sentinel UUID is presumably the fixed "the one admin" identity, matching blushbite.live's own `app/admin/` being single-shared-secret auth (`bb_admin_key` cookie = `CRON_SECRET`, see `app/api/admin/auth/route.ts:4-17`) — there is no per-admin-account identity system in either repo's admin surface as far as verified.
4. `app/dashboard/settings/page.tsx` already has a "Notifications" section (currently **uncommitted**, added this session) with one blanket push on/off toggle at roughly line 334–360, using local style tokens `S.section` / `S.sectionTitle` (see lines 291, 301, 338, 363 for the existing pattern — reuse it, don't invent a new visual pattern).
5. `app/api/companions/notifications/route.ts` already filters strictly by `recipient_type = 'companion' AND recipient_id = $1` (lines 12, 19, 35, 47) — i.e. companion notification reads are already scoped correctly; you are not touching that read path, only the send/preference-gate path.
6. Companion notification categories per the product spec (verbatim from the parent task): **3.1 Bookings — all booking-related notifications. 3.2 Interactions — all interactions with their profile.**

═══════════════════════════════════════════════════════════════════
TASK
═══════════════════════════════════════════════════════════════════

Step 1 — Migration (idempotent, additive only — do not drop and unconditionally recreate constraints blind; read current def first, union in new values, then replace):
  a. Add `'companion'` to `notification_preferences`'s `recipient_type` CHECK (`ALTER TABLE notification_preferences DROP CONSTRAINT ...; ADD CONSTRAINT ... CHECK (recipient_type IN ('admin','user','companion'))`).
  b. Add two boolean columns to `notification_preferences` for the companion categories, following the existing naming convention (`snake_case_enabled`, `NOT NULL DEFAULT true`):
     `bookings_enabled`, `interactions_enabled` — flag this name choice for review since it doesn't reuse `companion_bookings_enabled` (that column is scoped to *admin* seeing companion booking events, semantically different from a companion's own booking toggle — don't conflate the two, they're different rows with different `recipient_type`, but pick clearer column names than mine if you find the existing 4-column set ambiguous once you're looking at it directly).
  c. Confirm whether any new `notification_type` values are needed for companion-side categories or whether existing ones (`booking_request`, `booking_confirmed`, `booking_declined`, `booking_reminder`, `profile_viewed`, `story_like`, `story_save`, `story_comment`, `comment_reply`, `comment_like`) already cover 3.1/3.2 fully. They likely do — **flag, don't guess** if you find a companion-facing event with no matching existing type rather than silently inventing one that might collide with what the blushbite_user session independently adds for the admin/dreamer side of this same feature (this task is being executed as a split pair of prompts across two repos in parallel — assume no coordination channel between them beyond this migration step, so treat unknown-type additions to `notif_type_check` as unsafe to invent solo).
  d. Write this as a small one-off script under `scripts/` (follow the existing precedent: `scripts/migrate-push-companion.js`) rather than inline node -e, since this repo has no formal migrations folder.

Step 2 — `lib/notify.ts`:
  a. Add a helper (e.g. `getNotificationPreference(recipientType, recipientId, category)`) that reads `notification_preferences`, defaulting to `true` (enabled) if no row exists yet for that recipient — do not require every companion to have a pre-seeded preferences row.
  b. Gate `createNotification()` (or add a wrapping call site) so that booking-type and interaction-type notifications for companion recipients respect the new toggles before inserting. Do not silently swallow — if a notification is suppressed by preference, still consider whether analytics/audit needs the row (flag this design question rather than deciding unilaterally; a reasonable default is to skip the insert entirely rather than insert-but-suppress-push, since there's no "read but preference-hidden" concept in the UI today).

Step 3 — `app/api/companions/settings/route.ts` (GET/PATCH, per CLAUDE.md's API table): extend to read/write the two new companion preference columns (upsert into `notification_preferences` on PATCH, matching this repo's transaction pattern — `pool.connect()` + `BEGIN/COMMIT/ROLLBACK` per CLAUDE.md's Database section — when the row doesn't exist yet).

Step 4 — `app/dashboard/settings/page.tsx`: extend the existing "Notifications" section (don't create a second one) with two toggle switches — "Bookings" and "Interactions" — below the existing push on/off control, using the same `S.section`/`S.sectionTitle` inline-style pattern already in the file. Wire them to the settings route from Step 3.

═══════════════════════════════════════════════════════════════════
FLAG, DON'T GUESS
═══════════════════════════════════════════════════════════════════
- If any companion-side event needs a `notification_type` not already in the live CHECK list, stop and describe exactly which event and which value you'd propose, rather than adding it — the blushbite_user session may be adding an overlapping value concurrently and a collision/duplicate-migration is worse than a paused task.
- If the two new preference column names you chose (`bookings_enabled`/`interactions_enabled`) read as ambiguous once sitting next to the existing `companion_bookings_enabled`/`companion_subscriptions_enabled`/`content_interactions_enabled`/`new_content_enabled` columns in the same table, stop and propose clearer names instead of shipping the ambiguity.
- If `app/api/companions/settings/route.ts` doesn't have the shape described above (routes move), locate the actual GET/PATCH settings handler and describe what you found before proceeding.

═══════════════════════════════════════════════════════════════════
BRAND VOICE / DESIGN — quoted verbatim from this repo's CLAUDE.md
═══════════════════════════════════════════════════════════════════
- "CTAs: personal, present-tense. 'Begin your journey' / 'Enter my world' — never 'Submit' / 'Next' / 'Book now'"
- "NEVER use: 'Browse', 'Submit', 'Get started', 'Manage', 'Book now', 'Dashboard' as a heading"
- Toggle copy should read like the existing push section's copy ("Get a quiet nudge on this device when something happens...") — warm, first-person, not clinical switch labels like "Enable booking notifications."
- Dashboard pages use inline `style={}` objects, no Tailwind — do not introduce Tailwind classes into `app/dashboard/*`.
- Colors: rose accent `#e8607a`, dark bg `#0d1117`/`#111620`, border `#1c2333`, text `#eeeef0`/`#9ca3af`/`#6b7280` — reuse existing `S.*` tokens in the file rather than hardcoding new hex values.

═══════════════════════════════════════════════════════════════════
OUTPUT CONSTRAINTS
═══════════════════════════════════════════════════════════════════
- Create/modify files only inside this repo (`C:\Users\judob\dev\dev26\landingpagebb-`). Do not touch blushbite_user.
- Run the migration script once manually against the live DB as part of this task (it's additive/idempotent, safe to run) — but log exactly what you ran and its output in your final report.
- Run `npm run build` (or at minimum `npx tsc --noEmit`) before declaring done; report the result.
- If genuinely blocked on a decision (naming, whether to gate insert vs. gate push-only), stop and describe the blocker rather than guessing and shipping.
```

### Project: blushbite_user (`C:\Users\judob\dev\dev26\blushbite_user\apps\web`)
Scope: admin-side toggles (categories 1.1–1.4), dreamer-side toggles (categories
2.1–2.2), and the WhatsApp/Telegram lead-click tracking both depend on.

```
TASK: Build admin-side and dreamer-side granular notification preferences (categories 1.1–1.4 for Admin, 2.1–2.2 for Dreamers), including the WhatsApp/Telegram-click-as-lead-event tracking that categories 1.3/2.2 depend on, which does not exist yet anywhere in this codebase.

REPO: blushbite_user — C:\Users\judob\dev\dev26\blushbite_user\apps\web (consumer app: dreamers + admin panel both live here; pnpm monorepo, Drizzle ORM, TanStack Query, Framer Motion)

═══════════════════════════════════════════════════════════════════
PREREQUISITE — VERIFY LIVE DB STATE BEFORE WRITING ANY CODE
═══════════════════════════════════════════════════════════════════
The shared `notifications` / `notification_preferences` tables have already drifted from what
`db/schema.ts` in this repo describes, because a parallel session in blushbite.live
(a sibling repo, `C:\Users\judob\dev\dev26\landingpagebb-`, shares this same Railway Postgres
instance) has independently altered constraints live. Before writing any migration, confirm your
own `.env`/`.env.local` in this repo points at the SAME Postgres instance as blushbite.live
(verify — don't assume; both repos may or may not target one shared DB depending on current
env config), then re-run:

    SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
      WHERE conrelid = 'notifications'::regclass AND conname IN ('notif_type_check','notif_recipient_check');
    SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
      WHERE conrelid = 'notification_preferences'::regclass;
    SELECT * FROM notification_preferences LIMIT 10;

Do not trust the "ground truth" numbers below without this check — a parallel companion-side
prompt may have already added a `'companion'` value and/or new columns to `notification_preferences`
before this task runs.

═══════════════════════════════════════════════════════════════════
GROUND TRUTH — verified at prompt-generation time, sanity-check before depending on it
═══════════════════════════════════════════════════════════════════
1. Real admin panel lives HERE, not in blushbite.live. `apps/web/app/(admin)/admin/*` — nav groups for activity, ads, audio, bookings, bridges, companions, content, tags, users (`apps/web/app/(admin)/admin/layout.tsx`). Auth: `apps/web/lib/adminAuth.ts` — `requireAdmin(req)` checks an `admin_session` cookie against `process.env.ADMIN_SESSION_SECRET`, returns `{ ok: true, userId: 'admin' }` on success. **There is no per-admin-account identity** — it's a single shared password, same "one admin" model implied by the live seed row `notification_preferences.recipient_id = '00000000-0000-0000-0000-000000000001', recipient_type='admin'`. blushbite.live's CLAUDE.md references an admin panel at `C:\Users\Ravi Desai\Downloads\BlushBite\` which is a stale/unreachable reference — the real, current admin surface for this feature is the one described here. blushbite.live also has its own minimal `app/admin/` (boost/ads management only, different auth mechanism, `bb_admin_key` cookie) — do not confuse the two; this task's admin settings page belongs in THIS repo's admin panel, not that one.
2. `db/schema.ts:676-704` — `notifications` Drizzle model. Its local `notif_recipient_check` comment/definition currently says `sql\`${table.recipient_type} IN ('user','companion')\`` (line ~696) and `notifTypeCheck` (line ~700) lists 19 values, **missing `boost_purchased`** and missing `'admin'` in the recipient check — both of which already exist in the LIVE constraint (confirmed this run, see prerequisite section). Your migration should reconcile schema.ts to match live reality (add `'admin'` to the recipient check, add `'boost_purchased'` to the type check) as part of this feature's migration, not as an unrelated fix — note it explicitly in your migration commit/PR description since it's origin was a sibling-repo change, not something authored here.
3. `notification_preferences` table already exists live (created by the sibling repo's earlier session, not yet in this repo's `db/schema.ts` — add a matching Drizzle model as part of this task):
   ```
   id uuid PK, recipient_type varchar NOT NULL CHECK IN ('admin','user'[,'companion' — verify]),
   recipient_id uuid NOT NULL,
   content_interactions_enabled boolean NOT NULL DEFAULT true,   -- maps to category 1.1
   new_content_enabled boolean NOT NULL DEFAULT true,             -- maps to category 1.2
   companion_bookings_enabled boolean NOT NULL DEFAULT true,      -- maps to category 1.3
   companion_subscriptions_enabled boolean NOT NULL DEFAULT true, -- maps to category 1.4
   created_at, updated_at timestamptz, UNIQUE (recipient_type, recipient_id)
   ```
   This table's four existing columns already cover ALL FOUR admin categories (1.1–1.4) — you do not need new columns for admin, only new columns for dreamer categories 2.1 ("Interactions with their own content") and 2.2 ("Recommendation system — nearby new companions/content"). Follow the exact naming convention already set (`snake_case_enabled`) for the two new dreamer columns, e.g. `own_content_interactions_enabled`, `nearby_recommendations_enabled` — pick clearer names if these read ambiguous against the admin columns already in the table.
4. WhatsApp contact link — `app/(dreamer)/companions/[id]/page.tsx:286-307` — plain anchor: `href={\`https://wa.me/${data.whatsapp_number...}\`}`, **no onClick handler, no event logging of any kind**. Confirmed via repo-wide grep: **no Telegram link/button exists anywhere in this codebase.** The "click WhatsApp/Telegram as lead event" behavior specified in the parent task (categories 1.3 and 2.2's framing) requires building click tracking from scratch here — it is not "wire into an existing handler." If a Telegram contact channel doesn't exist as a companion-profile field at all (verify against `companion_profiles` schema before assuming), category coverage for "Telegram" clicks is moot until that field exists — flag it rather than fabricating a Telegram button.
5. Dreamer UI has no separate `/settings` route — `app/(dreamer)/profile/page.tsx` is the dreamer's account/profile hub (confirmed via `find`, only `profile/page.tsx` exists under `(dreamer)`). Existing fields on `UserProfile` interface there include `city`, `country` (lines ~34-35) — usable for category 2.2's "near their location" targeting without a schema change on the dreamer side.
6. Design tokens, quoted verbatim from `apps/web/CLAUDE.md` §7 (`## 7. DESIGN TOKENS (from social2.html :root — locked, do not change)`):
   ```
   #07090f   --bg        page root — darkest navy-black
   #0d1117   --bg2       modals, overlays, sheets
   #111620   --card      card surface
   #161d2a   --card2     nested card, inputs, hover states
   #e8607a   --accent    rose-pink — ALL CTAs, active states
   #c4485e   --accent2   accent on hover
   #c9a96e   --gold      verified badges, premium
   #eeeef0   --text      primary text
   #6b7280   --muted     secondary text, labels, placeholders
   #1c2333   --border    every border everywhere
   ```
   Banned CTA words, quoted verbatim from §1: `BAD: "Book a companion" / "Browse profiles" / "Sign up" / "Get started"` and `BAD: "Submit" / "Continue" / "View profile" / "Book now"`.
   §2 perf mandate: "60fps must be maintained at all times" — no non-GPU-composited animation properties (no animating `background-color` directly — use opacity transitions), see §2 for the full Framer Motion rules before animating any toggle switch.

═══════════════════════════════════════════════════════════════════
TASK
═══════════════════════════════════════════════════════════════════

Step 1 — Migration (Drizzle, in `apps/web/db/migrations/`, following this repo's existing migration convention — check `db/migrations/0000_mute_electro.sql` for the format):
  a. Reconcile `notifications` table constraints in `db/schema.ts` to match live: add `'admin'` to `notif_recipient_check`, add `'boost_purchased'` to `notifTypeCheck`.
  b. Add `notification_preferences` as a proper Drizzle model in `db/schema.ts` (it exists live but is undocumented in Drizzle — write the model to match the live shape from Ground Truth #3, do not `CREATE TABLE`, only add the Drizzle definition + a migration that's a no-op / guarded `IF NOT EXISTS` against the table itself, since it already exists).
  c. Add the two new dreamer-category boolean columns to `notification_preferences` (additive `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`).
  d. Determine whether new `notification_type` values are needed for: content creation/deletion events (category 1.2 — "not just interacted with"), audio play events (category 1.1's "plays"), and the new WhatsApp/Telegram lead-click event (categories 1.3/2.2). Existing values like `story_like`/`story_save`/`story_comment`/`comment_reply`/`comment_like` cover interaction-type events reasonably; there is nothing today for "content created/deleted" or "external contact click." Propose new values following the codebase's existing lowercase-snake-case convention (e.g. `content_created`, `content_deleted`, `contact_click_leaded` — or better names if you find the pattern used elsewhere) — but this is a design decision with real naming-collision risk against a parallel companion-side session in the sibling repo also touching this same constraint concurrently. **Flag your proposed values in your final report explicitly** rather than assuming they're uncontested.

Step 2 — Lead-click tracking (new — flag any assumption you have to make here):
  a. Add an `onClick` handler to the WhatsApp anchor in `app/(dreamer)/companions/[id]/page.tsx` (~line 288) that fires a fire-and-forget POST to a new route, e.g. `app/api/companions/[id]/contact-click/route.ts`, before/alongside the native link navigation (don't block or delay the `wa.me` navigation — this is an external link the browser is about to leave to).
  b. That route logs the event (new table or a row in an existing engagement-log table — check `db/schema.ts` for a generic engagement/event log table before creating a new one) AND calls `createNotification()`-equivalent logic to notify the admin recipient (category 1.3) with metadata capturing which companion, which "experience"/session_card the dreamer was viewing if determinable from context, and the fact that this is a lead/connection click, not a completed booking — do not label it as a booking notification.
  c. Telegram: only build the equivalent if you find a `telegram_handle`-shaped field on `companion_profiles` (or equivalent) once you look — per Ground Truth #4, none was found in this pass, so this is likely a "flag, don't guess" stop rather than something to build.

Step 3 — Preference-read helper + gating: add a Drizzle-based equivalent of "read `notification_preferences`, default enabled if no row" and use it to gate `db/helpers/createNotification.ts` (and any new admin-notification call sites from Step 2) — follow that file's existing pattern (fire-and-forget push wrapped in `try/catch`, never let push failures propagate, per lines 89-94 of the current file).

Step 4 — Admin settings UI: `app/(admin)/admin/layout.tsx`'s nav (see `NavItem`/`NavGroup` components in that file) has no "Settings" entry today — add one, plus a new page (e.g. `app/(admin)/admin/settings/page.tsx`) with four toggles matching categories 1.1–1.4, backed by `requireAdmin()` guard and reading/writing the sentinel admin row (`recipient_id = '00000000-0000-0000-0000-000000000001'`) in `notification_preferences`. Confirm that sentinel UUID is intentional (not a placeholder someone forgot to parameterize) before hardcoding it elsewhere — flag if uncertain.

Step 5 — Dreamer settings UI: add a "Notifications" section to `app/(dreamer)/profile/page.tsx` (or a natural sub-view within it — match how `EditProfileDrawer`/`TasteDrawer` are already used as focused sub-panels in that file, per its existing imports) with two toggles for categories 2.1/2.2, reading/writing the dreamer's own `notification_preferences` row via `recipient_type='user', recipient_id=<their user id>`.

═══════════════════════════════════════════════════════════════════
FLAG, DON'T GUESS
═══════════════════════════════════════════════════════════════════
- Any new `notification_type` value — report it explicitly, don't just add it silently, given the parallel sibling-repo session risk described above.
- Telegram click tracking if no Telegram field exists on companion profiles — stop, don't invent the field.
- Whether "which experience the user chose" (category 1.3's requirement) is derivable at all from current booking/session_card data — if `booking_requests` or session card selection isn't captured at the point of a WhatsApp click, say so rather than fabricating a value.
- If `ADMIN_SESSION_SECRET` / admin auth model turns out unsuitable for a "settings persist per admin" concept (since there's no per-admin-account row anywhere) — confirm the single-sentinel-row approach is acceptable rather than assuming it silently.
- If this repo's `.env`/`.env.local` DATABASE_URL does NOT point at the same instance as blushbite.live's, stop — the whole cross-repo constraint-drift narrative above is invalid and you're looking at a different database.

═══════════════════════════════════════════════════════════════════
OUTPUT CONSTRAINTS
═══════════════════════════════════════════════════════════════════
- Work only inside `apps/web` of this repo (`C:\Users\judob\dev\dev26\blushbite_user`). Use pnpm commands only (`pnpm --filter web ...` or run from inside `apps/web`), never npm.
- Follow Drizzle migration conventions already present in `apps/web/db/migrations/` — do not hand-write raw SQL migration files outside that structure.
- No Tailwind custom color names — inline hex per §7 ("ALWAYS inline hex — never custom Tailwind names").
- Respect the 60fps mandate (§2) for any new toggle/switch animation.
- Run `pnpm --filter web typecheck` (or the repo's equivalent lint/typecheck command — confirm exact script name in `package.json` before assuming) before declaring done; report the result.
- If genuinely blocked (Telegram field absence, notification_type collision risk, sentinel admin ID uncertainty), stop and describe the blocker rather than guessing and shipping.
```
